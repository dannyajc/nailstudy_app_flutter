import 'package:flutter/material.dart';

class SubjectParagraph extends StatelessWidget {
  const SubjectParagraph({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
